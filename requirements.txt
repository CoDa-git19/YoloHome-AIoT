# --- Web & API Server ---
Flask==3.0.3
python-dotenv==1.0.1

# --- Hardware & IoT ---
pyserial==3.5
paho-mqtt==1.6.1
schedule==1.2.1
adafruit-io==2.8.0

# --- Face Recognition Module ---
opencv-python==4.9.0.80
dlib>=19.24.6
face_recognition==1.3.0
scikit-learn==1.4.1.post1
numpy==1.26.4

# --- Speech Recognition Module ---
# YÊU CẦU HỆ THỐNG: cần ffmpeg trên PATH (KHÔNG cài được qua pip).
#   Windows: winget install Gyan.FFmpeg      (hoặc choco install ffmpeg)
#   Linux  : sudo apt-get install ffmpeg
#   macOS  : brew install ffmpeg
# Thiếu ffmpeg thì PhoWhisperSTT.transcribe() trả "" IM LẶNG, không báo lỗi.
soundfile==0.12.1
torch==2.0.0
transformers==4.38.0
sounddevice==0.5.5

# Chưa dùng tới:
# ctranslate2==4.1.0   # cho hướng faster-whisper (hiện dùng transformers)
# librosa              # stt_module đọc audio bằng soundfile + ffmpeg

# --- LLM Integration ---
# QUAN TRỌNG: pin đúng version đang chạy được trên máy bạn.
# Chạy: pip freeze | grep google-genai   rồi thay số bên dưới.
# Không pin -> mỗi người cài một version khác nhau, và API của SDK này
# thay đổi khá nhanh (response_schema, thinking_config...).
google-genai==2.11.0

# --- Testing ---
pytest